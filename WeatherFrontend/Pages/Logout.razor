@page "/logout"
@inject AuthService AuthService
@inject NavigationManager Navigation

@code {
    protected override async Task OnInitializedAsync()
    {
        await AuthService.Logout();
        Navigation.NavigateTo("/", forceLoad: true); // Force a full reload
    }
}
